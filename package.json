{
  "name": "test-stability-report-sync",
  "version": "1.0.0",
  "description": "This action syncs automated test results into the QualityForward Automated Test Stability Report.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint-test": "eslint --plugin eslint-plugin-jest --ignore-pattern 'dist/' --ignore-pattern 'src/' tests/",
    "lint": "eslint --ignore-pattern 'dist/' --ignore-pattern 'tests/' ."
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@actions/glob": "^0.5.0",
    "axios": "^1.7.7",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "eslint": "^9.10.0",
    "eslint-plugin-jest": "^28.8.3",
    "globals": "^15.9.0",
    "jest": "^29.7.0"
  }
}